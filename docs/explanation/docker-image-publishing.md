# Docker Image Publishing

This explanation outlines how the project publishes Docker images.

The project uses the Jib Maven plugin to publish the `cashu-mint-rest` Docker image to `docker.398ja.xyz/cashu-mint-rest`. Each release is tagged with both the current project version and `latest`.
