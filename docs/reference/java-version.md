# Java Version

This reference explains the required Java version for building and testing the project.

The project requires Java 21 for build and tests. The build enforces JDK version 21 via the Maven Enforcer plugin. Ensure your `JAVA_HOME` points to a JDK 21 installation, or configure a Maven Toolchain with JDK 21.

