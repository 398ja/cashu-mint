# Liquibase Changelog Placeholder

Liquibase change log files for the Cashu mint admin module will live here once migrations are authored. The default configuration
points to `classpath:db/changelog/db.changelog-master.yaml`, which should be created alongside additional changelog files when
Liquibase migrations are introduced.
