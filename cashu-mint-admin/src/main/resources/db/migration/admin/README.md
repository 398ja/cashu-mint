# Database Migrations

This directory hosts Flyway migration scripts for the Cashu mint admin module. It is intentionally empty for now and exists so
that the configured locations resolve without errors during development. Add migrations under the profile-specific directories
(e.g. `postgres` or `h2`) as they become available.
