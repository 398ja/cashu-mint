package xyz.tcheeric.cashu.mint.admin.presentation.lifecycle;

/**
 * Supported lifecycle actions shared across CLI and API layers.
 */
public enum LifecycleAction {
    CREATE,
    UPDATE,
    PAUSE,
    RESUME,
    RETIRE
}
