# Webhook Configuration

# HMAC secret for webhook signature validation
# If empty, signature validation is disabled (development mode only)
webhook.secret=${MINT_WEBHOOK_SECRET:}

# In production, set this environment variable:
# MINT_WEBHOOK_SECRET=your-secure-secret-here
