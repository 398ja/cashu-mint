# Written by the Maven build (resource filtering). The mint reports this as its
# NUT-06 `version`, so the string a wallet sees is always the artifact it is
# talking to. Never edit by hand.
mint.build.implementation=@project.parent.artifactId@
mint.build.version=@project.version@
